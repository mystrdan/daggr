# The URL of the deployed Cloudflare Worker (without trailing slash)
# Example: https://daggr-worker.your-subdomain.workers.dev
# In local dev, leave unset — Vite proxies /r/* to wrangler dev on localhost:8787
VITE_WORKER_URL=